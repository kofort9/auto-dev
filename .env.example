# Target repository for bugbot scanning and nightshift processing
TARGET_REPO=~/Repos/nonprofit-vetting-engine

# Nightshift state directory (state file, lock, logs, run artifacts)
STATE_DIR=~/.auto-dev

# Bugbot state directory
BUGBOT_STATE=~/.bugbot
